colors
fill the gaps
negative numbers
check each pixels z value currently using the drawlines pixels starting point z
params->loc should get its value changed depending on the screen size. 
there hsould also be min screen and max scale

-numbers might overrite zbuffer, maybe compare against absolute first

rename input files

leaks -atExit -- ./a.out

	
	//system("leaks fdf");
	//fscanf(stdin, "c"); // wait for user to enter input from keyboard

if (dest == NULL) //if ((str[0] == '-' && str[1] == '0') || dest == NULL)
		free_all(prm, head, "error", TRUE);
at

clamp color range